# firebase-poc
## Firebase Service
* react-firebaseui
* firebase
* firebase-admin

## Start server
* `cd server`
* `npm install && node index.js`

## Start client
* `cd client`
* `npm install && npm start`
